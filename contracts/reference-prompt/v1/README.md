# Source-owned rendered prompt parity v1

The parity record binds the immutable copied TidyCell prompt test and snapshot,
the network-denied relocated source replay, the candidate source closure, and
all 14 candidate prompt tests. The snapshot includes generate, repair, review,
provider-caching, ontology, candidate-range, and produced-CSV review payloads.

The copied and candidate snapshot bytes are identical. This is bounded
implementing-agent compatibility evidence over synthetic fixtures. It is not
independent review, provider dispatch authority, a reason to log raw prompts, or
full adversarial Phase C parity.
